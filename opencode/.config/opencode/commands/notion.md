---
description: Run Notion MCP read/write tasks with the low-cost notion-mcp subagent.
agent: notion-mcp
---

Execute the user's Notion MCP request.

User input:

```text
$ARGUMENTS
```

Requirements:
- Use Notion MCP tools for read and write operations.
- Keep existing Notion content unless the user explicitly asks for replacement.
- Return concise execution results and any follow-up actions needed.
