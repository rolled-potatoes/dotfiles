interface:
  display_name: "실제 작업"
  short_description: "승인된 태스크를 테스트 우선으로 구현합니다"
  default_prompt: "Use $workflow-implementation to implement the approved task with evidence for resuming later."
policy:
  allow_implicit_invocation: true
