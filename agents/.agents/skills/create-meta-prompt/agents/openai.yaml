interface:
  display_name: "Create Meta Prompt"
  short_description: "본 작업을 수행하지 않는 실행용 Agent 프롬프트 작성"
  default_prompt: "Use $create-meta-prompt to produce only an execution-ready Codex Agent prompt for the requested task; do not perform the task itself."

policy:
  allow_implicit_invocation: true
