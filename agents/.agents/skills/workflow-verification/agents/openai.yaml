interface:
  display_name: "작업 검증"
  short_description: "인수조건과 저장소 검증을 독립적으로 확인합니다"
  default_prompt: "Use $workflow-verification to verify the completed subtask and prepare reviewable drafts."
policy:
  allow_implicit_invocation: true
