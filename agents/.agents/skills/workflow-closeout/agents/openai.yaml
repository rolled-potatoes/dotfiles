interface:
  display_name: "작업 정리"
  short_description: "병합 뒤 상태와 worktree 정리를 안전하게 마무리합니다"
  default_prompt: "Use $workflow-closeout to prepare the approved merge follow-up and safe cleanup review."
policy:
  allow_implicit_invocation: true
